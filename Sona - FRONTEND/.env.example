# Copy this to .env.local for local development
# For production (Vercel), set VITE_BACKEND_URL in Vercel dashboard environment variables

# Your Render Node.js backend URL (no trailing slash)
VITE_BACKEND_URL=http://localhost:3000
